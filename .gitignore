# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
*.tsbuildinfo

# Coverage
coverage/
*.lcov

# Env files (never commit tokens or secrets)
.env
.env.local
.env.*.local
!.env.example

# Local MCP auth/config (token file lives in user home, but guard against accidental copies)
pulse-mcp-token.json
.pulse-mcp/

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Jest
.jest-cache/

# One-off smoke test scripts (not committed; use pnpm dev / pnpm test for proper suites)
scripts/

# Audit reports (sensitive: contain history-leak indices). Live in Confluence
# instead, with the page restricted to SG members.
audit-findings*.md
audit-*.md

